# Profile photo

Save a square image here as `avatar.jpg` and it appears in the bio section.
`.png` works too — just update the `avatar` path in `data/projects.js`.

If the file isn't here, the site shows your initials on a gradient instead.
